========================================
IMPORTANT: RESTART YOUR TERMINAL FIRST!
========================================

After installing Git, you MUST restart your terminal/PowerShell
for the changes to take effect.

========================================
STEPS TO PUSH TO GITHUB:
========================================

1. CLOSE this terminal/PowerShell window

2. OPEN a NEW terminal/PowerShell window

3. Navigate to your project:
   cd "D:\Intrution Detection System\Intrution Detection System"

4. Run ONE of these commands:

   Option A - Use the automated script:
   .\git_push_now.bat

   Option B - Manual commands:
   git init
   git config --global user.name "Silwalkg"
   git config --global user.email "your.email@example.com"
   git remote add origin https://github.com/Silwalkg/AI-powerd-IDS-System.git
   git add .
   git commit -m "Initial commit: AI-Powered IDS with training guides"
   git branch -M main
   git push -u origin main

========================================
AUTHENTICATION:
========================================

When prompted for credentials:
- Username: Silwalkg
- Password: Use Personal Access Token (NOT your GitHub password)

Create token at: https://github.com/settings/tokens
- Click "Generate new token (classic)"
- Select scope: repo (all checkboxes)
- Copy the token and use it as password

========================================
VERIFY GIT IS WORKING:
========================================

After restarting terminal, test with:
git --version

You should see something like:
git version 2.43.0.windows.1

========================================
